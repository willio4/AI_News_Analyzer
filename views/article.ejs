<%- include("partials/header.ejs") %>

<main class="max-w-3xl mx-auto px-4 py-8 text-black">
  <h1 class="text-3xl font-bold mb-2"><%= article.title %></h1>
  <p class="text-sm text-black mb-4">
    <%= article.source.name %> • <%= article.author || "Unknown author" %>
  </p>

  <% if (article.urlToImage) { %>
    <img src="<%= article.urlToImage || '/images/placeholder.jpg' %>" 
      alt="<%= article.title %>" 
      class="max-w-full h-auto"/>
  <% } %>
    <h2 class="font-bold text-lg">Description:</h2>
  <p class="leading-relaxed text-black"><%= article.description %></p>

  <h2 class="font-bold text-lg">Content:</h2>
  <p class="leading-relaxed text-black"><%= article.content %></p>

  <div class="flex justify-between">
    <a
      href="<%= article.url %>"
      target="_blank"
      class="text-center inline-block mt-6 text-green-500 underline"
    >
      Read original source
      </a>
    <a href="javascript:window.location.reload();" class="text-center inline-block mt-6 text-red-500 underline">Dig Deeper With AI</button>
    </a>
  </div>
</main>

<%- include("partials/footer.ejs") %>
